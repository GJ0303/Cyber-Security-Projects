# TOR Connection Checker (OSINT / Dark Web Research)

## 🎯 Objective
Verify whether traffic is routed through the Tor network.

---

## 🛠 Requirements
Install Tor Browser  
Install Python packages:
Run this in your virtual environment terminal:

pip install requests[socks]


If that fails, use:

pip install pysocks


(or both — safe)

pip install requests[socks] pysocks

---

## ▶️ Run
tor_checker.py

Shows whether IP is Tor-routed.

---

## 🔎 Why investigators use this
Dark web investigations require anonymity.

This tool helps verify safe routing before research.

---

## ⚠️ Legal
Use Tor only for learning + legitimate research.
